# PRD: 운동 대체 추천 서비스 (단일 화면 프로토타입)

## 1\) 목표와 성공 조건

**목표**
사용자가 운동 목표·싫은 운동·피하고 싶은 부위·운동 환경 4가지를 입력하면, 목표는 유지하면서 해당 부위/운동을 제외한 대체 운동을 추천해주는 단일 화면 웹 서비스를 만든다.

**성공 조건**

* 4개 입력값을 모두 입력하고 실행하면 5초 이내에 대체 운동 3\~5개가 화면에 출력된다.
* 출력된 운동에는 효과 크기 기준 1\~3위 순위가 표시된다.
* 각 운동에는 수행 방법(세트/횟수/주의사항)이 텍스트로 함께 표시된다.
* 로그인, 결제, DB 저장 없이 단일 화면 내에서 입력→결과 흐름이 완결된다.

\---

## 2\) 사용자 스토리

* 나는 무릎이 아픈 사용자로서, 무릎에 부담 없는 하체 운동을 추천받아 안전하게 운동을 시작하고 싶다.
* 나는 런닝이 싫은 사용자로서, 런닝을 제외하고도 유산소 목표를 달성할 수 있는 대체 운동을 알고 싶다.
* 나는 헬스장 기구가 없는 사용자로서, 집에서도 할 수 있는 대체 운동을 추천받고 싶다.
* 나는 추천 결과가 마음에 들지 않는 사용자로서, 입력을 수정해서 바로 다시 추천받고 싶다.

\---

## 3\) 화면 명세

**단일 화면 구성 (위→아래 1개 스크롤 화면)**

|영역|구성 요소|
|-|-|
|① 입력 영역|- 운동 목표 (선택형: 체중감량 / 근력강화 / 유연성 / 기타 직접입력)<br>- 싫은 운동 (텍스트 입력, 다중 가능)<br>- 피하고 싶은 부위 (텍스트 입력, 다중 가능)<br>- 운동 환경 (선택형: 홈트 / 헬스장 / 야외)|
|② 실행 버튼|"추천받기" 버튼 1개|
|③ 상태 표시 영역|로딩 중 문구, 입력 오류 문구, 재입력 안내 문구가 표시되는 공통 영역 (입력 영역 바로 아래 고정 위치)|
|④ 결과 영역|- 추천 운동 1\~3위 카드 형태로 나열<br>- 각 카드: 순위, 운동명, 세트/횟수, 주의사항 텍스트<br>- 카드 하단에 "다시 추천받기" 버튼|

화면 전환은 없으며, 모든 상태(입력→로딩→결과→재요청)는 한 화면 안에서 영역 ③, ④의 내용만 바뀌는 방식으로 처리한다.

\---

## 4\) 기능 명세

**기능명: 부위/운동 회피 기반 대체 운동 추천**

|단계|동작|
|-|-|
|1|사용자가 4개 입력값(목표, 싫은 운동, 피할 부위, 환경)을 입력한다|
|2|"추천받기" 클릭 시 입력값 유효성을 먼저 검사한다 (5장 참고)|
|3|유효성 통과 시, 입력값 기준으로 규칙 기반 매칭 테이블에서 조건에 맞는 운동 후보를 필터링한다 (싫은 운동 제외, 피할 부위에 부담 가는 운동 제외, 환경에서 수행 불가능한 운동 제외)|
|4|필터링된 후보 중 목표 달성 효과가 큰 순으로 정렬하여 상위 3개를 선정한다|
|5|선정된 운동 각각에 대해 세트/횟수/주의사항 텍스트를 매칭하여 결과 영역에 순위와 함께 출력한다|
|6|사용자가 "다시 추천받기" 클릭 시 입력값은 유지한 채 3\~5단계를 재실행한다 (새 결과로 교체)|

세션 내 입력값은 임시로만 유지되며, 새로고침 시 초기화된다. (DB 저장 없음)

\---

## 5\) 예외 처리

### 5-1. 입력이 비어있을 때

* **조건**: 4개 입력값 중 하나라도 비어있는 상태로 "추천받기" 클릭
* **동작**: 결과 영역(④)으로 넘어가지 않고, 상태 표시 영역(③)에 "○○ 항목을 입력해주세요" 형태로 비어있는 항목명을 구체적으로 지목하여 안내 문구를 출력한다.
* **복구**: 사용자가 해당 항목을 채우고 다시 클릭하면 정상 진행한다.

### 5-2. 입력이 너무 추상적일 때

* **조건**: "피하고 싶은 부위" 등 입력값이 매칭 테이블 기준 상위 카테고리 수준으로 넓은 경우 (예: "다리")
* **동작**: 결과를 생성하지 않고, "'다리'는 범위가 넓어요. 허벅지 / 종아리 / 발목 중 더 구체적으로 입력해주세요"와 같이 하위 선택지를 제시하며 재입력을 요청한다.
* **복구**: 사용자가 하위 카테고리로 재입력 후 다시 클릭하면 정상 진행한다.
* **비고**: 추상도 판단은 사전에 정의된 부위/운동 카테고리 트리(대분류-중분류)를 기준으로 하며, 대분류 단어가 입력되면 이 예외로 처리한다.

### 5-3. 입력이 너무 자세하고 좁을 때

* **조건**: 입력값이 매칭 테이블이 인식하는 최소 단위보다 더 세부적인 경우 (예: "손바닥 가운데서 아랫부분")
* **동작**: 결과를 생성하지 않고, "입력이 너무 세부적이에요. '손목' 정도로 조금 더 넓게 입력해주세요"와 같이 한 단계 상위 카테고리를 제안하며 재입력을 요청한다.
* **복구**: 사용자가 상위 카테고리로 재입력 후 다시 클릭하면 정상 진행한다.

### 5-4. 추천 로직(매칭) 처리가 실패했을 때

* **조건**: 입력값은 유효하지만 매칭 과정에서 결과 생성에 실패한 경우 (예: 조건에 맞는 운동 후보가 0개로 필터링되어 결과를 만들 수 없는 경우 포함)
* **동작**: 상태 표시 영역에 "추천을 만들지 못했어요. 입력 내용을 다시 확인하고 입력해주세요"를 출력하고, 결과 영역은 비운 채로 입력 영역으로 포커스를 되돌린다.
* **복구**: 사용자가 입력값(특히 싫은 운동/피할 부위)을 수정하고 재요청한다.

### 5-5. 응답(처리)이 지연될 때

* **조건**: "추천받기" 클릭 후 결과 출력까지 일정 시간 이상 소요되는 경우
* **동작**: 상태 표시 영역에 로딩 인디케이터와 함께 "조금만 기다려주세요" 문구를 표시한다. 결과가 준비되면 로딩 표시는 사라지고 결과 영역이 채워진다.
* **복구**: 별도 사용자 조작 불필요, 자동 전환.

### 5-6. 결과가 마음에 들지 않을 때 (재요청)

* **조건**: 사용자가 결과를 확인한 후 다른 추천을 받고 싶은 경우
* **동작**: 결과 카드 하단의 "다시 추천받기" 버튼 클릭 시, 기존 입력값을 유지한 상태로 3\~5단계(매칭\~출력)를 재실행하여 결과 영역을 새 내용으로 교체한다.
* **복구**: 즉시 재실행되며 별도 오류 처리 불필요. 단, 재실행 결과도 5-4 상황(후보 0개)이 발생할 수 있으므로 동일하게 처리한다.

\---

## 6\) 완료 조건 (Definition of Done)

* \[ ] 단일 화면에서 입력(4개 항목) → 추천받기 → 결과(1\~3위) 흐름이 새로고침 없이 동작한다
* \[ ] 5-1\~5-6 예외 상황 6가지가 모두 화면에서 재현 및 확인 가능하다
* \[ ] 로그인/결제/DB 저장 기능이 코드 어디에도 존재하지 않는다
* \[ ] 결과는 순위 + 운동명 + 세트/횟수 + 주의사항 텍스트로 구성되어 출력된다
* \[ ] "다시 추천받기"로 입력값 유지한 채 재요청이 가능하다
* \[ ] 전체 기능이 5시간 작업 범위 내에서 구현 완료된다

